name: frontend_flutter
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.6.0
  provider: ^6.1.5+1
  shared_preferences: ^2.5.5
  dio: ^5.9.2
  supabase_flutter: ^2.12.2
  fl_chart: ^1.2.0
  intl: ^0.20.2
  image_picker: ^1.2.1
  connectivity_plus: ^7.1.0
  flutter_launcher_icons: ^0.14.4

  # Excel: importar y exportar registros
  excel: ^4.0.6
  file_picker: ^10.1.9
  syncfusion_flutter_xlsio: ^29.2.11
  open_filex: ^4.7.0
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true